Codepen project CSS +HTML: https://codepen.io/wylie25/pen/yLOyxrL

 1. What is Semantic HTML?: A semantic element clearly describes its meaning. ie:<header> and <footer>

    2. What is HTML used for?: to markup text and imgs into groups for clearer frameworks on a wepage. also alows for the creation of hypertext links.

    3. What is an attribute and where do we put it?: provides information about HTML elements and goes inside of a tag.

    4. What is the h1 tag used for? How many times should I use it on a page?: The h1 tag marks the main title on the page.

    5. Name two tags that have required attributes: <img> and <alt>.

    6. What do we put in the head of our o HTML document?: the Title (<h1>) and any navigations.

    7. What is an id?: Gives a unique identity to an html element tag.
 
    8. What elements can I add an id to?: Any of the HTML elements.

    9. How many times can I use the same id on a page? : only once.

    10. What is a class?: Specifies one or more elements are grouped together.
 
    11. What elements can I add a class to?: Can be added to any element.

    12. How many times can I use the same class on a page?: As many times as you need to.
 
    13. How do I get my link to open in a new tab?: Just add a target="url" attribute to your links anchor tag.

    14. What is the alt attribute in the image tag used for?: provides an alternate image text for text readers or if the image can't load.

    15. How do I reference an id?: .the name of your id

    16. What is the difference between a section and a div?: A section refers to multiple elements inside the section tags. While div refes to a singular element contained within it.

    17. What is CSS used for?: To style and add presentation to the web page.

    18. How to we select an element? Example - every h2 on the page: h2{}.

    19. What is the difference between a class and an id? - Give me an example of when I might use each one: A class refers to multiple tags grouped together by a common class name. While an id marks a singular tag.

    20. How do we select classes in CSS?: .the name of the class

    21. How do we select a p element with a single class of “human””? .human p{}

    22. What is a parent child selector? When would this be useful?: Selects all elements that are a direct child of the specified element. Any time you wanted to alter or add to any of the children under a parent.

    23. How do you select all links within a div with the class of sidebar?: .sidebar div{}

    24. What is a pseudo selector?: Selects elements that are in a specific state.

    25. What do we use the change the spacing between lines?: line-height: #;

    26. What do we use to change the spacing between letters?: letter-spacing: #;

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?: text-transform: capitalize; ortext-transform: lowercase;

    28. How do I add a 1px border around my div that is dotted and black?: div{border: 1px dotted black;}

    29. How do I select everything on the page?: *{}

    30. How do I write a comment in CSS?: /* The text of your comment */

    31. How do I find out what file I am in, when I am using the command line?: PWD
 
    32. Using the command line - how do I see a list of files/folders in my current folder?: li

    33. How do I remove a file via the command line? Why do I have to be careful with this?: rm and you have to be careful because their will be no confirmmation the file will just be permenatly deleted.

    34. Why should I use version control?: It tracks every individual change by each contributor and helps prevent work from conflicting.
 
    35. How often should I commit to github?: Anytime you make any new changes to your work.

    36. What is the command we would use to push our repo up to github?: git push

    37. Walk me through Lambda's git flow.: step 1 fork the assignment. step 2 add collaberators. step 3 clone the assignment in gitbash step 4 branch the assignment in gitbash. step 5 commit work in gitbash. step 6 push work back to repo in gitbash. step 7 submit pull request to TL in github to submit finished work.
 

Stretch Questions

    1. What is the difference between an inline element and a block element?: Inline elements keep things on the same line and Block elements usually create a break in the line.

    2. What happens when an element is positioned absolutely?: it moves the element to its own layer in the layout.
 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width?: by using display: inline-block element.

    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default:
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 